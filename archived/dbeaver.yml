services:
  dbeaver:
    image: dbeaver/cloudbeaver:latest
    container_name: dbeaver
    restart: always
    ports:
      - "8978:8978"
    volumes:
      - $APPDATA/cloudbeaver:/opt/cloudbeaver/workspace

networks:
  dbeaver:
    name: dbeaver
    external: true
